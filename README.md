# Extracción de entidades nombradas

Este plugin usa la librería de SpaCy para procesamiento de texto y detección de entidades nombradas. Es necesario tener un modelo compatible para el procesamiento.

## Instalación

Es necesario crear una carpeta **models** en la carpeta del plugin y copiar ahí la carpeta del modelo.